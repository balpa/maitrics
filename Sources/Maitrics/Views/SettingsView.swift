import SwiftUI
import MaitricsCore

struct SettingsView: View {
    let settings: AppSettings
    var body: some View {
        Text("Settings — coming soon")
            .frame(width: 450, height: 500)
    }
}
