import SwiftUI
struct UsageTrendChartView: View {
    let dailyTotals: [(date: Date, tokens: Int)]
    let allDailyTotals: [(date: Date, tokens: Int)]
    var body: some View { Text("Chart placeholder").padding(20) }
}
